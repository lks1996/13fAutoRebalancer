spring:
    application:
        name: "13FAutoRebalancer"

    jpa:
        hibernate:
            ddl-auto: update  # (create, create-drop, update, none ? ??)
        show-sql: true
        properties:
            hibernate:
                format_sql: true


logging:
    level:
        root: INFO
        org.hibernate.type.descriptor.sql: trace